import { test } from "node:test";
import assert from "node:assert/strict";

// Parser tests will be filled in during Phase 1 once `parser.ts` exists.
// Keeping one placeholder test so `npm test` exits 0 during Phase 0.

test("phase-0 placeholder", () => {
  assert.equal(1 + 1, 2);
});
